/**
 * Test Sitewide Membership Restrictions
 *
 * @since [version]
 */

import { test, expect } from '@wordpress/e2e-test-utils-playwright';

test.describe( 'SitewideMembershipRestrictions', () => {

	// Placeholder: original test was stubbed out / not implemented.
	test( 'should not allow logged out users to view the homepage', async ( { page } ) => {
		// TODO: Implement when sitewide membership restriction test is fleshed out.
	} );

} );
