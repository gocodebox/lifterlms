name: post_type
in: query
description: Filter results to those of a specific `post_type`.
required: false
schema:
  type: string
  enum:
    - course
    - lesson
    - llms_membership
    - llms_question
    - llms_quiz
    - section
  default: course
