/**
 * Returns the WordPress-loaded version of Backbone for use with things that need it and use Require.
 * @return   obj
 * @since    [version]
 * @version  [version]
 */
define( function() {
	return Backbone;
} );
